<?php

declare(strict_types=1);

// This test is a no-op test on purpose. It exists for the sake of parity
// with the Test262 test suite.
it('does not support HasProperty')
    ->expect(true)
    ->toBe(true);
